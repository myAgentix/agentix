# CRIE-015: Kernel Conflicts, Redundancies, Integration Efficiencies

**Date**: 2026-07-29
**Issue**: myAgentix/agentix#164
**Commit**: a3f48dd

## Summary

14 of 15 findings resolved across 14 files, 4 new files created. ~204 lines saved.
I-2 (config pipeline consolidation) deferred — boot-path risk disproportionate to benefit.

## New Files

| File | Purpose |
|------|---------|
| `src/agentixd/routes/_errors.py` | Typed HTTPException factories: `not_found`, `validation_error`, `service_unavailable` |
| `src/agentixd/routes/_kernel_dep.py` | `get_kernel(request)` — single seam for route-layer kernel access |
| `src/agentixd/_paths.py` | `ensure_dir()` — path creation utility |
| `src/agentix/tools/_empty_args_guard.py` | `_empty_args_guard`, `_field_type_hint`, constants moved from dispatcher |

## Actions

### Conflicts (C-1 – C-4)

| ID | File | Change | Lines saved |
|----|------|--------|-------------|
| C-1/C-2 | `_config.py`, `main.py` | `resolve_config_path()` added; `lifespan()` and `_resolve_cfg_path()` delegate to it — 3 inline duplications → 1 function | ~15 |
| C-3 | `health.py`, `sessions.py` | Bare `getattr(kernel, "_readiness_hook", None)` and `getattr(kernel, "_pre_turn_hook", None)` replaced with direct field access | ~4 |
| C-4 | `admin.py` | `_mutate_config_yaml(cfg_path, mutator)` helper; `install_driver` and `uninstall_driver` use it | ~8 |

### Redundancies (R-1 – R-8)

| ID | File | Change | Lines saved |
|----|------|--------|-------------|
| R-1 | `admin.py`, `sessions.py`, `scaffold.py` | 23 inline `HTTPException(status_code=4xx)` calls replaced with factory helpers | ~30 |
| R-2 | `sessions.py` | `_deserialize_app_meta()` helper; used in `_session_row_to_dict` | ~4 |
| R-3 | `catalog.py`, `admin.py` | `SkillBundle.to_dict()` added; `_skill_layer()` + `_bundle_to_dict()` removed from admin | ~15 |
| R-4 | `_kernel.py`, `admin.py` | `is_user_skill_root(path)` predicate; used in build loop and list_skill_roots | ~4 |
| R-5 | `sessions.py` | `_get_or_resume_session()` helper; `run_turn` delegates to it | ~8 |
| R-6 | `agent_dispatcher.py` → `tools/_empty_args_guard.py` | Guard function + constants extracted; dispatcher imports from new module | ~80 |
| R-7 | `types.py`, `agent_dispatcher.py` | `ToolCallResult.to_json_content()` added; `_tool_result_to_content()` removed | ~6 |
| R-8 | `main.py`, `_paths.py` | `ensure_dir()` utility; inline mkdir in `_start_daemon` uses it | ~6 |

### Integration Efficiencies (I-1, I-3)

| ID | File | Change | Lines saved |
|----|------|--------|-------------|
| I-1 | `health.py`, `sessions.py`, `admin.py` | `get_kernel(request)` from `_kernel_dep.py` replaces raw `request.app.state.kernel` | ~10 |
| I-3 | `health.py`, `sessions.py` | Covered by C-3 — direct field access replaces getattr pattern | — |

### Skipped

| ID | Reason |
|----|--------|
| I-2 | Config pipeline consolidation — touches boot path; risk > benefit |

## Test Results

148 unit tests passed. Pre-existing failures in driver/storage/sync tests are due to
optional `openai`/`minio` extras not installed in the venv — unrelated to these changes.
